# <NNNN>. <Title: the decision, as a noun phrase>

<Architecture Decision Record. One decision: the situation, the options, the choice, the reasoning, the reversal condition.>

- Date: <YYYY-MM-DD>
- Status: accepted
- Supersedes: <NNNN or none>

<This record is an archive. After acceptance, only the Status line changes ("superseded by NNNN", "deprecated"). To change the decision, write a new ADR.>

## What to do

<what a reader does about this; "nothing, it is already in the build" is an answer>

## Context

<The situation that forced a choice. What constraint, requirement, or failure made this necessary. Point at where the pressure shows: `<path>`.>

## Options

<Each option considered, one paragraph, including the one chosen. What it would mean, what it costs, what it risks. Two options minimum — a decision with one option is a fact.>

### <option A>

### <option B>

## Decision

<What was chosen and in one or two sentences why it beat the others. Point at where it is implemented: `<path>`.>

## Consequences

<What this makes easier, what it makes harder, what it forecloses. Be specific about the costs — the next reader is looking for the trap.>

## Sources

<Where the reasoning above comes from. One line per source: `handoff`, `<path>:<symbol>` (a decision comment in the code), a prior ADR, or `inferred`. In author mode, delete this section — you are the source. In subagent mode it is required, and an ADR whose only source is `inferred` carries Status `accepted (unconfirmed)`.>

## What would reverse this

<The concrete condition under which this should be revisited. A measurement, a requirement change, a dependency's status. "If needs change" is not an answer.>
